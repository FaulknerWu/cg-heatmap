[project]
name = "cg-heatmap"
version = "0.1.0"
description = "Coinglass Liquidation Heatmap Screenshot Actor"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "apify>=2.0.0",
    "playwright>=1.49.0",
]
